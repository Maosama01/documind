info:
  name: List Documents (Protected)
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/documents/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
