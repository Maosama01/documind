info:
  name: Login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8000/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
