info:
  name: Login
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "tanishq@gmail.com",
        "full_name": "Tanishq Khandelwal",
        "password": "mypassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
