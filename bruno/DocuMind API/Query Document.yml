info:
  name: Query Document
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/query/
  body:
    type: json
    data: |-
      {
        "question": "What are the payment terms?",
        "document_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
